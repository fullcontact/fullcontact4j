package com.fullcontact.api.libs.fullcontact4j.enums;

public enum Casing {
    /**
     * Results are always full UPPERCASE
     */
    UPPERCASE,
    /**
     * Results are always full lowercase.
     */
    LOWERCASE,
    /**
     * Results are always in TitleCase
     */
    TITLECASE;
}
